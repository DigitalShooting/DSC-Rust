language: rust
rust:
  - stable
  - beta
  - nightly
cache: cargo
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
  include:
    - env: COMPILER=clang++-7 GSL_CXX_STANDARD=17
      addons: *clang70
